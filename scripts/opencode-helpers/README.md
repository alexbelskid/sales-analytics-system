# 🤖 OpenCode Agents Automation Scripts

Автоматизированные помощники для работы с агентами OpenCode.

## 📋 Оглавление

1. [Быстрый старт](#быстрый-старт)
2. [Доступные скрипты](#доступные-скрипты)
3. [Полный цикл разработки](#полный-цикл-разработки)
4. [Примеры использования](#примеры-использования)

---

## 🚀 Быстрый старт

### Установка

Скрипты уже готовы к использованию! Просто убедись что они исполняемые:

```bash
chmod +x scripts/opencode-helpers/*.sh
```

### Как это работает

Эти скрипты **генерируют готовые промпты** для OpenCode агентов. Вы просто копируете промпт и вставляете в чат OpenCode.

---

## 📦 Доступные скрипты

### 1. `full-dev-cycle.sh` - Полный цикл разработки

**Что делает:** Проводит через все 4 этапа разработки (Research → Architecture → Decomposition → Development)

**Использование:**
```bash
./scripts/opencode-helpers/full-dev-cycle.sh "Создать API для управления задачами"
```

**Что происходит:**
1. Генерирует промпт для `@research-solution`
2. После завершения исследования → промпт для `@architecture-designer`
3. После проектирования → промпт для `@decomposition`
4. Подсказывает как выполнять задачи с `execute-task.sh`

---

### 2. `execute-task.sh` - Выполнение одной задачи

**Что делает:** Помогает выполнить конкретную задачу и автоматически делает git commit

**Использование:**
```bash
./scripts/opencode-helpers/execute-task.sh 001 python
./scripts/opencode-helpers/execute-task.sh 005 csharp
./scripts/opencode-helpers/execute-task.sh 003 typescript
```

**Аргументы:**
- `001` - номер задачи (ищет файл `task-001-*.md` в `docs/tasks/`)
- `python` - язык (python, csharp, typescript) - определяет какой developer агент использовать

**Что происходит:**
1. Находит файл задачи в `docs/tasks/`
2. Генерирует промпт для соответствующего developer агента
3. После выполнения показывает измененные файлы
4. Предлагает сделать git commit с автоматическим сообщением
5. Подсказывает номер следующей задачи

---

### 3. `quick-research.sh` - Быстрое исследование

**Что делает:** Генерирует промпт для быстрого исследования

**Использование:**
```bash
# Исследование решений
./scripts/opencode-helpers/quick-research.sh solution "Как реализовать кэширование в FastAPI?"

# Исследование кода
./scripts/opencode-helpers/quick-research.sh codebase "Как работает аутентификация в этом проекте?"

# Веб-поиск
./scripts/opencode-helpers/quick-research.sh web "LangChain streaming best practices"
```

**Типы исследования:**
- `solution` - использует `@research-solution`
- `codebase` - использует `@research-codebase`
- `web` - использует `@research-web`

---

## 🔄 Полный цикл разработки

### Пример: Добавление новой функции

#### Шаг 1: Запуск полного цикла

```bash
./scripts/opencode-helpers/full-dev-cycle.sh "Добавить экспорт данных в CSV"
```

Скрипт выведет промпт:
```
@research-solution

Задача: Добавить экспорт данных в CSV

Проведи исследование:
1. Какие технологии и библиотеки использовать?
2. Какие есть готовые решения и best practices?
...
```

**Действие:** Скопируй промпт → Вставь в OpenCode → Дождись результата → Нажми Enter

#### Шаг 2: Архитектура

Скрипт автоматически покажет следующий промпт:
```
@architecture-designer

На основе результатов исследования спроектируй архитектуру...
```

**Действие:** Скопируй → Вставь в OpenCode → Дождись ADR документа → Нажми Enter

#### Шаг 3: Декомпозиция

```
@decomposition

Разбей реализацию на задачи:
Добавить экспорт данных в CSV
...
```

**Действие:** Скопируй → Вставь в OpenCode → Дождись создания файлов задач → Нажми Enter

#### Шаг 4: Выполнение задач

Теперь у тебя есть файлы:
```
docs/tasks/csv-export/
  task-001-add-csv-library.md
  task-002-create-export-service.md
  task-003-add-api-endpoint.md
  task-004-frontend-export-button.md
  task-005-tests.md
```

Выполняй по очереди:

```bash
# Задача 1
./scripts/opencode-helpers/execute-task.sh 001 python
# Копируешь промпт в OpenCode → Выполняешь → Нажимаешь Enter → Автокоммит

# Задача 2
./scripts/opencode-helpers/execute-task.sh 002 python
# Повторяешь процесс

# Задача 3
./scripts/opencode-helpers/execute-task.sh 003 python

# Задача 4 (frontend)
./scripts/opencode-helpers/execute-task.sh 004 typescript

# Задача 5
./scripts/opencode-helpers/execute-task.sh 005 python
```

**Результат:** Каждая задача = 1 коммит. История git чистая и понятная.

---

## 💡 Примеры использования

### Пример 1: Новичок создает To-Do приложение

```bash
# 1. Запускаем полный цикл
./scripts/opencode-helpers/full-dev-cycle.sh "Создать To-Do список с регистрацией пользователей"

# 2. Следуем промптам (Research → Architecture → Decomposition)

# 3. Выполняем задачи по очереди
./scripts/opencode-helpers/execute-task.sh 001 python  # Настройка проекта
./scripts/opencode-helpers/execute-task.sh 002 python  # Модели БД
./scripts/opencode-helpers/execute-task.sh 003 python  # API регистрации
./scripts/opencode-helpers/execute-task.sh 004 python  # API для задач
./scripts/opencode-helpers/execute-task.sh 005 typescript  # Фронтенд компоненты
./scripts/opencode-helpers/execute-task.sh 006 python  # Тесты
```

### Пример 2: Быстрый вопрос по коду

```bash
./scripts/opencode-helpers/quick-research.sh codebase "Как организована работа с базой данных?"
# Копируешь промпт → Получаешь ответ
```

### Пример 3: Исследование новой технологии

```bash
./scripts/opencode-helpers/quick-research.sh web "LangGraph multi-agent patterns"
# Агент найдет документацию и примеры
```

### Пример 4: Оптимизация существующего кода

```bash
# Сначала исследуем что можно улучшить
./scripts/opencode-helpers/quick-research.sh codebase "Какие медленные запросы к БД есть в проекте?"

# Потом запускаем цикл разработки
./scripts/opencode-helpers/full-dev-cycle.sh "Оптимизировать медленные SQL запросы"
```

---

## 🎯 Workflow для новичков

### Ежедневная работа

**Утро:**
```bash
# Выбираешь что делать сегодня
./scripts/opencode-helpers/full-dev-cycle.sh "Добавить функцию X"
# Следуешь промптам
```

**День:**
```bash
# Выполняешь задачи по очереди
./scripts/opencode-helpers/execute-task.sh 001 python
./scripts/opencode-helpers/execute-task.sh 002 python
# И так далее
```

**Вечер:**
```bash
# Проверяешь что все работает
git log --oneline  # Смотришь свои коммиты за день
pytest  # Запускаешь тесты
```

---

## 📝 Структура проекта после использования скриптов

```
project/
├── docs/
│   ├── arch/
│   │   └── decisions/
│   │       ├── ADR-0001-user-management.md
│   │       └── ADR-0002-csv-export.md
│   └── tasks/
│       ├── user-management/
│       │   ├── README.md
│       │   ├── task-001-setup.md
│       │   └── task-002-models.md
│       └── csv-export/
│           ├── README.md
│           └── task-001-csv-library.md
├── scripts/
│   └── opencode-helpers/
│       ├── full-dev-cycle.sh
│       ├── execute-task.sh
│       └── quick-research.sh
└── (твой код)
```

---

## 🎓 Советы

### ✅ DO

- **Используй скрипты последовательно** - Research → Architecture → Decomposition → Development
- **Делай коммит после каждой задачи** - скрипт `execute-task.sh` поможет
- **Читай созданные документы** - ADR и Task файлы содержат важную информацию
- **Задавай вопросы через `quick-research.sh`** - агенты твои помощники

### ❌ DON'T

- **Не пропускай Research** - иначе можешь выбрать неправильный подход
- **Не делай несколько задач в одном коммите** - теряется история изменений
- **Не игнорируй декомпозицию** - большие задачи сложно выполнить правильно
- **Не меняй структуру файлов задач вручную** - пусть агенты это делают

---

## 🔧 Кастомизация

### Изменить агента по умолчанию

Отредактируй `execute-task.sh`, строка:
```bash
LANGUAGE="${2:-python}"  # Замени python на csharp или typescript
```

### Добавить свой скрипт

Создай новый скрипт в `scripts/opencode-helpers/` и сделай исполняемым:
```bash
chmod +x scripts/opencode-helpers/my-script.sh
```

---

## ❓ FAQ

**Q: Скрипты автоматически запускают агентов?**  
A: Нет, они генерируют промпты которые ты копируешь в OpenCode. Это сделано специально, чтобы ты видел что происходит и мог корректировать процесс.

**Q: Можно ли запускать агентов автоматически?**  
A: OpenCode пока не поддерживает полную автоматизацию. Но скрипты максимально упрощают процесс.

**Q: Что если я хочу изменить промпт?**  
A: Скрипты выводят промпт на экран - просто скопируй и отредактируй перед вставкой в OpenCode.

**Q: Как добавить новый тип агента?**  
A: Отредактируй `execute-task.sh`, добавь новый `case` в секции определения агента.

---

## 🎉 Готово!

Теперь у тебя есть мощная система автоматизации работы с агентами OpenCode. Начни с простой задачи и привыкай к процессу!

**Первый шаг:**
```bash
./scripts/opencode-helpers/full-dev-cycle.sh "Твоя первая задача"
```

Удачи! 🚀
