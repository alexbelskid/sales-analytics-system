# 🔧 Alterini AI - Решение проблем

Здесь собраны решения частых проблем при запуске и работе системы.

---

## 1. Backend не запускается

### Ошибка: "ModuleNotFoundError: No module named 'xxx'"

**Причина**: Не установлены зависимости

**Решение**:
```bash
cd backend
source venv/bin/activate  # или создай: python3 -m venv venv
pip install -r requirements.txt
```

---

### Ошибка: "SUPABASE_URL not found in environment"

**Причина**: Не настроен файл .env

**Решение**:
```bash
cd backend
cp .env.example .env
# Открой .env и заполни все переменные
```

---

### Ошибка: "Address already in use (port 8000)"

**Причина**: Порт 8000 уже занят другим процессом

**Решение**:
```bash
# Найди и убей процесс на порту 8000
lsof -i :8000
kill -9 <PID>

# Или запусти на другом порту
uvicorn app.main:app --port 8001
```

---

### Ошибка: "Permission denied: ./start.sh"

**Причина**: Скрипт не имеет прав на выполнение

**Решение**:
```bash
chmod +x start.sh
./start.sh
```

---

## 2. Frontend не видит Backend

### Ошибка: "net::ERR_CONNECTION_REFUSED"

**Причина**: Backend не запущен

**Решение**:
```bash
# В одном терминале:
cd backend && ./start.sh

# В другом терминале:
cd frontend && npm run dev
```

---

### Ошибка: "Failed to fetch" или "CORS error"

**Причина**: Неправильный API URL или проблемы с CORS

**Решение**:
1. Проверь что в `frontend/.env.local` правильный URL:
   ```
   NEXT_PUBLIC_API_URL=http://localhost:8000
   ```
2. Перезапусти frontend: `npm run dev`

---

### Ошибка: URL показывает "undefined/api/..."

**Причина**: Переменная окружения не установлена

**Решение**:
```bash
cd frontend
cp .env.example .env.local
# Убедись что NEXT_PUBLIC_API_URL установлен
npm run dev
```

---

## 3. Ошибки загрузки CSV

### Ошибка: "422 Unprocessable Entity"

**Причина**: Неверный формат CSV файла

**Решение**:
- Проверь что файл в формате UTF-8
- Проверь что есть все обязательные колонки
- Сравни с примером в `backend/test_data/sales_test.csv`

Обязательные колонки для sales:
```
date,customer_name,product_name,quantity,price,amount
```

---

### Ошибка: "500 Internal Server Error" при загрузке

**Причина**: Проблема с подключением к базе данных

**Решение**:
1. Проверь что Supabase проект активен
2. Проверь правильность `SUPABASE_URL` и `SUPABASE_KEY` в .env
3. Проверь логи backend в терминале

---

### Данные загрузились, но не отображаются

**Причина**: Возможно данные за другой период

**Решение**:
1. Смените период на дашборде (выпадающий список)
2. Обновите страницу
3. Проверьте даты в CSV файле

---

## 4. AI не генерирует ответы

### Ошибка: "500 Internal Server Error" при генерации

**Причина**: Не настроен или неверный Gemini API ключ

**Решение**:
1. Проверь `GOOGLE_GEMINI_API_KEY` в .env
2. Получи новый ключ на https://aistudio.google.com/app/apikey
3. Перезапусти backend

---

### Ошибка: "API key not valid"

**Причина**: Неверный формат API ключа

**Решение**:
- Ключ должен начинаться с `AIza...`
- Не должно быть пробелов в начале/конце
- Проверь что скопировал полный ключ

---

### AI генерирует пустые или странные ответы

**Причина**: Нет данных в базе знаний

**Решение**:
1. Перейди в "AI Ассистент" → "База знаний"
2. Добавь информацию о продуктах и компании
3. Попробуй сгенерировать снова

---

## 5. Проблемы с Supabase

### Ошибка: "JWT expired" или "Invalid token"

**Причина**: Истёк или неверный API ключ

**Решение**:
1. Зайди в Supabase Dashboard
2. Settings → API
3. Скопируй новый anon/public key
4. Обнови `SUPABASE_KEY` в .env

---

### Таблицы не найдены

**Причина**: База данных не инициализирована

**Решение**:
1. Зайди в Supabase Dashboard → SQL Editor
2. Выполни SQL для создания таблиц (спроси у разработчика)

---

## 6. Проблемы с Production Deploy

### Railway: "Application failed to respond"

**Причина**: Приложение не стартует

**Решение**:
1. Проверь логи в Railway Dashboard
2. Убедись что все переменные окружения установлены
3. Проверь что `PORT` не указан (Railway сам назначает)

---

### Vercel: "500 Internal Server Error"

**Причина**: Неверный `NEXT_PUBLIC_API_URL`

**Решение**:
1. Vercel Dashboard → Project → Settings → Environment Variables
2. Убедись что `NEXT_PUBLIC_API_URL` указывает на Railway URL
3. Redeploy проект

---

## 🆘 Всё ещё не работает?

1. Проверь логи backend в терминале
2. Открой DevTools в браузере (F12) → Console
3. Запусти тесты: `cd backend && python test_api.py`

Если ничего не помогает - проверь что все шаги из `LAUNCH_CHECKLIST.md` выполнены.
