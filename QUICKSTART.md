# 🚀 Быстрый старт приложения

## Управление процессами

### Запустить все процессы
```bash
./start.sh
```

Этот скрипт:
- ✅ Очищает старые процессы на портах 3000 и 8000
- ✅ Запускает бэкенд FastAPI на http://127.0.0.1:8000
- ✅ Запускает фронтенд Next.js на http://localhost:3000
- ✅ Логирует вывод в `backend.log` и `frontend.log`

### Проверить статус
```bash
./status.sh
```

Показывает:
- 📊 Статус бэкенда и фронтенда (запущен/не запущен)
- 🔢 PID процессов
- 🌐 URL для доступа
- ✅ Проверку доступности API

### Остановить все процессы
```bash
./stop.sh
```

## URL сервисов

После запуска доступны:

| Сервис | URL | Описание |
|--------|-----|----------|
| **Фронтенд** | http://localhost:3000 | Основное приложение Next.js |
| **Бэкенд API** | http://127.0.0.1:8000 | FastAPI REST API |
| **API Docs (Swagger)** | http://127.0.0.1:8000/docs | Интерактивная документация API |
| **ReDoc** | http://127.0.0.1:8000/redoc | Альтернативная документация API |

## Логи

### Просмотр логов в реальном времени

```bash
# Бэкенд
tail -f backend.log

# Фронтенд
tail -f frontend.log

# Оба сразу (в двух терминалах)
tail -f backend.log
tail -f frontend.log
```

### Очистка логов

```bash
# Очистить все логи
rm -f backend.log frontend.log

# Логи пересоздадутся автоматически при следующем запуске
```

## Ручной запуск (если нужно)

### Бэкенд (FastAPI)
```bash
cd backend
source ../.venv/bin/activate
uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

### Фронтенд (Next.js)
```bash
cd frontend
npm run dev
```

## Архитектура

```
┌─────────────────────────────────────────────────────┐
│                   ПОЛЬЗОВАТЕЛЬ                      │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────────┐
        │    Frontend (Next.js)       │
        │   http://localhost:3000     │
        │                             │
        │  - React 18                 │
        │  - TypeScript               │
        │  - Tailwind CSS             │
        │  - Recharts                 │
        └──────────────┬──────────────┘
                       │ HTTP API Calls
                       ▼
        ┌─────────────────────────────┐
        │   Backend (FastAPI)         │
        │  http://127.0.0.1:8000      │
        │                             │
        │  - Python 3.9+              │
        │  - FastAPI                  │
        │  - Uvicorn                  │
        │  - SQLAlchemy               │
        └──────────────┬──────────────┘
                       │
                       ▼
        ┌─────────────────────────────┐
        │    Supabase (PostgreSQL)    │
        │                             │
        │  - База данных              │
        │  - Storage                  │
        │  - Auth                     │
        └─────────────────────────────┘
```

## Переменные окружения

### Backend (.env в корне проекта)
```bash
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
OPENAI_API_KEY=your_openai_key
GROQ_API_KEY=your_groq_key
```

### Frontend (frontend/.env.local)
```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## Устранение неполадок

### Порт уже занят
```bash
# Проверить, что использует порт
lsof -i :8000  # для бэкенда
lsof -i :3000  # для фронтенда

# Освободить порт
./stop.sh
```

### Бэкенд не запускается
```bash
# Проверить логи
tail -100 backend.log

# Убедиться, что виртуальное окружение активировано
source .venv/bin/activate

# Проверить зависимости
pip install -r requirements.txt
```

### Фронтенд не запускается
```bash
# Проверить логи
tail -100 frontend.log

# Переустановить зависимости
cd frontend
rm -rf node_modules package-lock.json
npm install
```

## Полезные команды

```bash
# Перезапустить всё
./stop.sh && ./start.sh

# Проверить статус
./status.sh

# Тестовый запрос к API
curl http://127.0.0.1:8000/docs

# Просмотреть процессы
ps aux | grep -E "(uvicorn|node.*dev)" | grep -v grep
```

## Разработка

### Горячая перезагрузка
- **Бэкенд**: Автоматически перезагружается при изменении `.py` файлов (флаг `--reload`)
- **Фронтенд**: Автоматически перезагружается при изменении файлов в `src/`

### Тестирование API
Используйте Swagger UI: http://127.0.0.1:8000/docs

### Структура проекта
```
.
├── backend/          # FastAPI бэкенд
│   ├── app/
│   │   ├── main.py      # Точка входа
│   │   ├── routers/     # API endpoints
│   │   ├── services/    # Бизнес-логика
│   │   └── models/      # Модели данных
│   └── requirements.txt
├── frontend/         # Next.js фронтенд
│   ├── src/
│   │   ├── app/         # Pages (App Router)
│   │   ├── components/  # React компоненты
│   │   └── lib/         # Утилиты
│   └── package.json
├── start.sh          # 🚀 Запуск всех процессов
├── stop.sh           # 🛑 Остановка всех процессов
├── status.sh         # 📊 Проверка статуса
├── backend.log       # Логи бэкенда
└── frontend.log      # Логи фронтенда
```

---

**Создано**: 27 января 2026
**Автор**: Python Senior Developer Agent
