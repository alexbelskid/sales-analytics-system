# ✅ ВСЁ ГОТОВО К ЗАПУСКУ!

Все файлы созданы и настроены. Осталось несколько простых шагов.

---

## 📋 Следующие действия:

### 1. Настрой Backend

```bash
cd backend
cp .env.example .env
```

Открой файл `.env` и заполни ключи:
- `SUPABASE_URL` — из Supabase Dashboard → Settings → API
- `SUPABASE_KEY` — там же (anon/public key)
- `GOOGLE_GEMINI_API_KEY` — из https://aistudio.google.com/app/apikey

### 2. Запусти Backend

```bash
cd backend
./start.sh
```

### 3. Настрой Frontend (в новом терминале)

```bash
cd frontend
cp .env.example .env.local
```

### 4. Запусти Frontend

```bash
npm run dev
```

### 5. Открой в браузере

👉 **http://localhost:3000**

---

## 🧪 Проверь что всё работает

- [ ] Дашборд открывается
- [ ] Sidebar показывает "Alterini AI"
- [ ] Можно переключаться между страницами

---

## 📁 Созданные файлы

| Файл | Описание |
|------|----------|
| `backend/requirements.txt` | Все зависимости Python |
| `backend/.env.example` | Шаблон переменных окружения |
| `backend/start.sh` | Скрипт быстрого запуска |
| `backend/test_data/` | Тестовые CSV файлы |
| `backend/test_api.py` | Скрипт тестирования API |
| `backend/README.md` | Документация backend |
| `frontend/.env.example` | Шаблон для frontend |
| `LAUNCH_CHECKLIST.md` | Полный чеклист запуска |
| `TROUBLESHOOTING.md` | Решение проблем |
| `QUICKSTART.md` | Быстрый старт (3 шага) |

---

## 🐛 Если что-то не работает

Открой файл `TROUBLESHOOTING.md` — там собраны решения всех частых проблем.

---

## 🚀 Ready to go!

Удачи с Alterini AI! 🎉
