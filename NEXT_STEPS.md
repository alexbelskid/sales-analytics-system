# ✅ SQL КОД СКОПИРОВАН В БУФЕР ОБМЕНА!

## 🎯 Следующие шаги:

### 1. Вернитесь в Supabase SQL Editor
Вы уже там: https://supabase.com/dashboard/project/hnunemnxpmyhexzcvmtb/sql

### 2. Очистите редактор
Удалите текст `.venv/bin/python scripts/fix_rpc_quick.py` из редактора

### 3. Вставьте правильный SQL
Нажмите **Cmd+V** (или Ctrl+V)

Должен появиться SQL код, начинающийся с:
```sql
-- ============================================================================
-- Analytics RPC Functions
-- These functions provide optimized aggregations...
```

### 4. Нажмите RUN
Кнопка "Run" в правом верхнем углу редактора

### 5. Дождитесь успеха
Вы увидите сообщение "Success" и результаты выполнения.

---

## 📊 Что вы увидите после успешного выполнения:

В разделе "Results" должно появиться:
- ✅ Functions dropped (3 строки)
- ✅ Functions created (3 функции)
- ✅ Permissions granted (3 строки)
- ✅ Schema reloaded

---

## ⚠️ Важно!

**НЕ** запускайте команды bash/python в SQL Editor!

SQL Editor принимает **только SQL код**, не команды терминала.

---

## 🔄 После успешного выполнения:

Вернитесь в терминал и выполните:

```bash
# Перезапустите backend
./stop.sh && ./start.sh

# Проверьте результат
./scripts/diagnose.sh
```

Ошибка "column reference total_revenue is ambiguous" должна исчезнуть! ✅

---

## 📞 Нужна помощь?

Если что-то пошло не так:

1. **Проверьте, что вы в SQL Editor** (не в терминале)
2. **SQL уже в буфере обмена** - просто нажмите Cmd+V
3. **Если потеряли SQL** - откройте файл: `COPY_THIS_SQL.txt`

---

**SQL готов к вставке!** Нажмите Cmd+V в Supabase SQL Editor! 🚀
