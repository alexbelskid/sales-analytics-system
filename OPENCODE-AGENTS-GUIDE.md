# 🤖 АВТОМАТИЧЕСКИЕ АГЕНТЫ OPENCODE - РУКОВОДСТВО

## ✅ ВСЁ ГОТОВО! Теперь вы можете работать БЕЗ ТЕРМИНАЛА

У вас установлено **4 агента-оркестратора**, которые автоматически управляют другими агентами.

---

## 🎯 ГЛАВНЫЕ АГЕНТЫ (используй их в чате OpenCode)

### 1️⃣ `@auto-workflow` - Полный цикл создания новой функции

**Что делает:**
- Автоматически проводит через все 4 этапа разработки
- Вызывает агентов: Research → Architecture → Decomposition
- Создает план в виде файлов задач
- Всё происходит автоматически!

**Как использовать:**

Просто напиши в чате OpenCode:

```
@auto-workflow Создать систему регистрации пользователей с email и паролем
```

**Что произойдет:**
1. 🔍 Агент запустит исследование (@research-solution)
2. 🏗️ Спроектирует архитектуру (@architecture-designer)
3. 📋 Разобьёт на задачи (@decomposition)
4. ✅ Покажет готовый план разработки

**Результат:**
- Создан ADR документ в `docs/arch/decisions/`
- Созданы файлы задач в `docs/tasks/`
- Готов план действий

---

### 2️⃣ `@task-executor` - Выполнение одной задачи

**Что делает:**
- Находит файл задачи
- Вызывает нужного developer агента
- Автоматически делает git commit
- Предлагает следующую задачу

**Как использовать:**

```
@task-executor 001 python
```

или просто

```
@task-executor 001
```

(язык определится автоматически)

**Аргументы:**
- `001` - номер задачи
- `python` - язык (python/csharp/typescript) - опционально

**Что произойдет:**
1. 🔍 Найдёт файл `task-001-*.md`
2. 📖 Прочитает задачу
3. 🤖 Вызовет developer агента
4. ✅ Создаст код
5. 💾 Сделает git commit автоматически

---

### 3️⃣ `@auto-dev` - Выполнить ВСЕ задачи автоматически

**Что делает:**
- Находит все задачи в проекте
- Выполняет их ПО ОЧЕРЕДИ
- Делает commit после каждой
- Показывает прогресс

**Как использовать:**

```
@auto-dev
```

**Что произойдет:**
1. 🔍 Найдёт все задачи
2. 📊 Покажет план (например: 5 задач, ~7 часов)
3. ❓ Спросит подтверждение
4. 🚀 После "да" - выполнит ВСЕ задачи подряд
5. 🎉 Покажет итоговый summary

**⚠️ Внимание:** Это может занять время! Агент выполнит ВСЕ задачи.

---

### 4️⃣ `@quick-ask` - Быстрые вопросы

**Что делает:**
- Анализирует ваш вопрос
- Автоматически выбирает правильного research агента
- Перенаправляет вопрос
- Показывает ответ

**Как использовать:**

```
@quick-ask Как работает аутентификация в этом проекте?
```

```
@quick-ask Как лучше реализовать кэширование?
```

```
@quick-ask Найди документацию по FastAPI
```

**Агент сам определит куда отправить:**
- Вопросы о коде → `@research-codebase`
- Вопросы о решениях → `@research-solution`
- Поиск документации → `@research-web`

---

## 🎬 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Создание калькулятора (новичок)

**Открой чат OpenCode и напиши:**

```
@auto-workflow Создать простой калькулятор с операциями +, -, *, / на Python
```

**Что произойдет автоматически:**

1. ✅ Исследование (агент найдет best practices)
2. ✅ Архитектура (создаст структуру проекта)
3. ✅ Задачи (разобьет на простые шаги)

**Результат:**
```
docs/tasks/calculator/
  task-001-project-setup.md
  task-002-calculator-logic.md
  task-003-user-interface.md
  task-004-tests.md
```

**Дальше выполни задачи:**

```
@task-executor 001 python
```

Подожди завершения, потом:

```
@task-executor 002 python
```

И так далее.

**ИЛИ выполни всё сразу:**

```
@auto-dev
```

---

### Пример 2: Добавление новой функции в существующий проект

**Напиши:**

```
@auto-workflow Добавить экспорт данных в CSV формат
```

**Агент автоматически:**
- Изучит существующий код
- Предложит архитектуру
- Создаст задачи
- Учтёт существующие паттерны проекта

**Выполни задачи:**

```
@task-executor 001 python
@task-executor 002 python
@task-executor 003 python
```

---

### Пример 3: Быстрый вопрос

**Напиши:**

```
@quick-ask Где в коде находится логика авторизации?
```

Агент сам определит что это вопрос о коде, вызовет `@research-codebase` и покажет ответ.

---

### Пример 4: Полная автоматизация

**Создай план:**

```
@auto-workflow Создать блог с постами, комментариями и админкой
```

**Выполни всё автоматически:**

```
@auto-dev
```

Агент спросит подтверждение, после "да" - выполнит ВСЁ сам!

---

## 📚 ПОЛНЫЙ СПИСОК АГЕНТОВ

### Автоматические оркестраторы (новые):
- `@auto-workflow` - Полный цикл разработки
- `@task-executor` - Выполнение задачи
- `@auto-dev` - Автоматическое выполнение всех задач
- `@quick-ask` - Умный роутер вопросов

### Планирование:
- `@research-solution` - Исследование решений
- `@research-codebase` - Исследование кода
- `@research-web` - Веб-поиск
- `@architecture-designer` - Архитектура
- `@decomposition` - Декомпозиция
- `@creative` - Креативные решения
- `@minimalist-ai-architect` - Минималистичная архитектура

### Разработка:
- `@python-senior-developer` - Python код
- `@csharp-senior-developer` - C# код
- `@typescript-vue-developer` - TypeScript/Vue код

### Вспомогательные:
- `@code` - Вопросы по коду

---

## 🎯 РЕКОМЕНДОВАННЫЙ WORKFLOW

### Для новичков:

```
1. @auto-workflow "Моя идея"
   ↓ Жди результата (план готов)

2. @task-executor 001 python
   @task-executor 002 python
   @task-executor 003 python
   ↓ Выполняй по очереди

3. Готово! 🎉
```

### Для опытных:

```
1. @auto-workflow "Идея"
   ↓ План готов

2. @auto-dev
   ↓ Подтверди
   ↓ Жди завершения всех задач

3. Проверь, протестируй, готово!
```

### Для быстрых вопросов:

```
@quick-ask "Любой вопрос"
```

---

## ⚡ ЧАСТЫЕ СЦЕНАРИИ

### Я хочу создать новую программу

```
@auto-workflow Опиши что хочешь создать
```

### Я хочу добавить функцию в существующий проект

```
@auto-workflow Опиши функцию
```

### У меня есть вопрос

```
@quick-ask Твой вопрос
```

### Я хочу выполнить конкретную задачу

```
@task-executor 001
```

### Я хочу всё автоматом

```
@auto-workflow Моя идея
@auto-dev
```

---

## 🔥 МОЩНЫЕ КОМБИНАЦИИ

### Комбо 1: Полный автомат

```
@auto-workflow Создать To-Do приложение
```

Дождись плана, потом:

```
@auto-dev
```

Всё! Можешь идти пить кофе ☕

---

### Комбо 2: Контролируемая разработка

```
@auto-workflow Создать API для блога
```

Потом выполняй задачи по одной:

```
@task-executor 001 python
```

Проверяешь результат, если ок:

```
@task-executor 002 python
```

И так далее.

---

### Комбо 3: Исследование + Разработка

```
@quick-ask Как лучше реализовать real-time уведомления?
```

Изучаешь ответ, потом:

```
@auto-workflow Добавить real-time уведомления через WebSocket
```

Выполняешь:

```
@auto-dev
```

---

## ❓ FAQ

**Q: Нужно ли открывать терминал?**
A: Нет! Всё работает в чате OpenCode.

**Q: Агенты сами пишут код?**
A: Да! Агенты-оркестраторы вызывают developer агентов, которые пишут код.

**Q: Создаются ли git коммиты автоматически?**
A: Да! `@task-executor` и `@auto-dev` делают коммиты автоматически.

**Q: Могу ли я остановить @auto-dev?**
A: Да, процесс можно остановить. При ошибке агент сам остановится и спросит что делать.

**Q: Как узнать что создано?**
A: Агенты показывают пути к файлам и список изменений.

**Q: Можно ли изменить созданный план?**
A: Да, файлы задач в `docs/tasks/` можно редактировать вручную.

---

## 🎓 ДЛЯ НОВИЧКОВ

### Твой первый проект за 5 минут

**1. Открой OpenCode (VSCode с OpenCode)**

**2. Напиши в чате:**

```
@auto-workflow Создать программу которая приветствует пользователя по имени
```

**3. Дождись окончания (агент создаст план)**

**4. Выполни задачи:**

```
@task-executor 001 python
```

Дождись выполнения.

```
@task-executor 002 python
```

**5. Готово! Проверь свой код!**

---

## 🚀 ГОТОВО!

Теперь ты можешь разрабатывать программы **БЕЗ ТЕРМИНАЛА**, просто общаясь с агентами в чате OpenCode!

### Начни прямо сейчас:

```
@auto-workflow Твоя первая идея
```

---

**Удачи в программировании!** 🎉💪

*P.S. Если что-то непонятно - спроси:*
```
@quick-ask Как пользоваться агентами OpenCode?
```
